services:
  interactive-drawer:
    build: .
    container_name: interactive-drawer
    restart: unless-stopped
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    environment:
      - PORT=${PORT:-3001}
      # Uncomment and set when behind a reverse proxy:
      # - BASE_URL=https://draw.example.com
